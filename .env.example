# [변경사유]: 카카오톡 PC 수집 — 환경 예시 (쿠키·세션 저장 금지)
# kakao-import-local 경로 (chats/photos 산출)
KAKAO_IMPORT_ROOT=F:/site_kdance/TEST_web/kakao-import-local

# 카톡 사진 다운로드 고정 폴더
KAKAO_DOWNLOAD_DIR=D:/Users/msgu/Documents/카카오톡 받은 파일

# 수집 후 kakao-import 호출 (1=호출, 0=파일만)
KAKAO_COLLECT_RUN_IMPORT=1

# [변경사유]: import(run·poster·similar-detect) 완료 직후 upload --no-dry-run
# 1=스케줄용(수집 끝나면 바로 업로드, similar deferred 는 hold). 기본 0=기존과 동일
KAKAO_COLLECT_RUN_UPLOAD=0

# [변경사유]: I5 — 실행 후 관리자 카톡 요약 전송 (비우면 전송 안 함)
# 친구 탭에서 검색할 닉네임 — 채팅 탭 통합검색 금지(오픈채팅·방명 혼입)
# 창 제목에 이 문자열이 포함될 때만 전송. 전송 후 채팅 탭으로 자동 복귀
# KAKAO_ADMIN_NOTIFY_SEARCH=댄스인포관리자

LOG_LEVEL=INFO
